# Copyright (c) 2013 The Chromium Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# GNU Makefile based on shared rules provided by the Native Client SDK.
# See README.Makefiles for more details.

VALID_TOOLCHAINS := pnacl glibc clang-newlib linux
NACL_SDK_ROOT ?= $(abspath $(CURDIR)/../..)

TARGET = jsoncpp

include $(NACL_SDK_ROOT)/tools/common.mk

LIBS = $(DEPS)

CFLAGS = -Wall -Wno-strict-aliasing
CFLAGS += -DJSON_USE_EXCEPTION=0
SOURCES = json_reader_fix.cpp \
  json_value.cpp \
  json_writer.cpp


all: install

# Build rules generated by macros from common.mk:

$(foreach src,$(SOURCES),$(eval $(call COMPILE_RULE,$(src),$(CFLAGS))))
$(eval $(call LIB_RULE,jsoncpp,$(SOURCES)))
ifneq (,$(findstring $(TOOLCHAIN),glibc))
$(eval $(call SO_RULE,jsoncpp,$(SOURCES),$(LIBS),$(DEPS)))
endif

